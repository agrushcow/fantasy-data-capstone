{
  "name": "fantasy-data-capstone",
  "version": "0.0.0",
  "description": "A chat example to showcase how to use `socket.io` with a static `express` server with `async` for control flow.",
  "main": "server.js",
  "scripts": {
    "test": "mocha"
  }
  "repository": "",
  "author": "Adam Grushcow",
  "dependencies": {
    "async": "~0.2.8",
    "express": "^3.2.6",
    "socket.io": "~0.9.14"
  },
  "devDependencies": {
    "mocha": "^3.0.2"
  }
}
